import { StyleSheet } from "react-native";

const styles = StyleSheet.create({ 
    paintName: {
        bottom: 40, 
        left: 10, 
        position: 'absolute', 
        color: 'white',
        fontWeight: 'bold',
        fontSize: 16,
      },

      artistName: {
        bottom: 20, 
        left: 10,
        position: 'absolute',
        color: 'white',
        fontSize: 16,
      }
})

export default styles;